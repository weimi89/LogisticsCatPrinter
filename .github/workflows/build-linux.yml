name: Build Linux Binary

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build (Ubuntu 22.04 amd64)
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            pkg-config \
            libssl-dev \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libsoup-3.0-dev \
            libjavascriptcoregtk-4.1-dev

      - name: Install Rust (stable)
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry & target
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target

      - name: Build release binary
        working-directory: src-tauri
        run: cargo build --release

      - name: Stage release binary
        run: |
          mkdir -p dist
          cp src-tauri/target/release/gkconfig-ui dist/LogisticsCatPrinter-linux-x64
          chmod +x dist/LogisticsCatPrinter-linux-x64
          ls -lh dist/LogisticsCatPrinter-linux-x64
          file dist/LogisticsCatPrinter-linux-x64

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: LogisticsCatPrinter-linux-x64
          path: dist/LogisticsCatPrinter-linux-x64
          if-no-files-found: error
          retention-days: 30

      - name: Attach to Release (on tag)
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/LogisticsCatPrinter-linux-x64
          fail_on_unmatched_files: true
